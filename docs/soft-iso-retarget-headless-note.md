# Soft-iso retarget — headless note (no glance PASS claim)

Programmer will glance + ship URL. Metrics only.

## What changed (0.1 fix pass)
- **Lattice kill:** absolute outward fatten (EX≈2.8) + land-coloured stroke + turf underfill under clip; flat fills (no per-quad gradients); heavier colour blur.
- **Rolling land:** multi-lobe shared-vertex heightfield (peak ~1.0+, interior spread ~0.7); HEIGHT_SCALE 68.
- **Raise live:** pick score init `-Infinity` + found flag + fallback (was rejecting clicks when `h*0.15-d < 0`); crest stroke moves silhouette (~50 CSS px in probe).
- Fit foot restored to silhouette width (~70% phone width); loaf sides + stream ribbon kept; no props.

## Headless checks
- `npm run build` PASS
- `node scripts/verify-sculpt.mjs` PASS — peakDelta ≥0.18

## Do not push / Pages — Programmer glances then ships.
